plugins {
    id 'java'
    id 'org.springframework.boot' version '3.1.0'
    id 'io.spring.dependency-management' version '1.1.0'
    id 'io.freefair.lombok' version '6.6.1'
}

group = 'com.cvbuilder'
version = '1.0.0'
sourceCompatibility = '17'

repositories {
    mavenCentral()
}

ext {
    jjwtVersion = '0.11.5'
    seleniumVersion = '4.11.0'
}

dependencies {
    // === SPRING BOOT STARTER'ları ===
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    
    // === VERİTABANLARI ===
    implementation 'com.h2database:h2'
    runtimeOnly 'org.postgresql:postgresql'
    
    // === JWT (Güvenlik) ===
    implementation "io.jsonwebtoken:jjwt-api:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-impl:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-jackson:${jjwtVersion}"
    
    // === SCRAPING & AYRIŞTIRMA ===
    implementation 'org.jsoup:jsoup:1.17.2' 
    implementation 'org.apache.commons:commons-lang3:3.12.0'

    // === PDF OLUŞTURMA ===
    implementation 'com.itextpdf:itext7-core:7.2.5'
    
    // === GELİŞTİRME ARAÇLARI ===
    developmentOnly 'org.springframework.boot:spring-boot-devtools'
    
    // === TEST ===
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'org.springframework.security:spring-security-test'
    testImplementation 'org.testcontainers:junit-jupiter'
    testImplementation 'org.testcontainers:postgresql'
    testImplementation 'org.springframework.boot:spring-boot-testcontainers'
}

// Java derleyicisine parametre isimlerini koruması talimatını verir.
// Bu sayede Spring Data JPA ve Dependency Injection hataları önlenir.
tasks.withType(JavaCompile) {
    options.compilerArgs << '-parameters'
}

test {
    useJUnitPlatform()
}

bootRun {
    systemProperties = System.properties
}

bootJar {
    archiveFileName = 'cv-builder-backend.jar'
    launchScript()
}